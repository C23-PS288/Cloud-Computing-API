const register = require('./register');
const login = require('./login');
const getUser = require('./getUser');
const checkAllUser = require('./checkAllUser');
const logout = require('./logout');

module.exports = {
  register,
  login,
  getUser,
  checkAllUser,
  logout,
}